// local modules
import type { LanguageData } from './seed-language.types';

export const languageRUData = { code: 'ru' } as const satisfies LanguageData;
export const languageENData = { code: 'en' } as const satisfies LanguageData;
