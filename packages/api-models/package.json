{
  "name": "@repo/api-models",
  "version": "0.0.0",
  "private": true,
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint",
    "build": "tsup src/index.ts --format cjs,esm --dts --sourcemap",
    "postinstall": "yarn build"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3"
  }
}
