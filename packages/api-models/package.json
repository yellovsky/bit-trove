{
  "name": "@repo/api-models",
  "version": "0.0.0",
  "private": true,
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint",
    "build": "tsup src/index.ts --format cjs,esm --dts --sourcemap",
    "postinstall": "yarn build"
  },
  "devDependencies": {
    "eslint": "^9.19.0",
    "tsup": "^8.3.6",
    "typescript": "^5.7.3"
  }
}
